export default function Page() {
    return (
      <section className="mx-auto max-w-3xl p-6">
        <h1 className="text-2xl font-semibold">Setmore++ (Booking + Ops)</h1>
        <p className="mt-2 text-sm opacity-80">
          Smart availability, deposits, reminders, admin flags. Demo mode on.
        </p>
        {/* your demo UI goes here */}
      </section>
    );
  }